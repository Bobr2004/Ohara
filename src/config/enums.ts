enum ModalsEnum {
   uploadPdf = "uploadPdf",
   uploadAudio = "uploadAudio",
   auth = "auth",
   avatars = "avatars",
   signOut = "signOut",
   error = "error",
   confirm = "confirm"
}

export { ModalsEnum };
